import { createMaterialTheme } from '@rootnative/core/create-theme'

// Seeded from the blue at the centre of the rootnative hero gradient.
export const { lightTheme, darkTheme } = createMaterialTheme('#60A5FA', {
  variant: 'vibrant',
})
